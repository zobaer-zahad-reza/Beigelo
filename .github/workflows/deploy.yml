# Workflow-এর নাম
name: Deploy Website to VPS

on:
  push:
    branches:
      - main # শুধুমাত্র main ব্রাঞ্চে পুশ করলেই এটি রান হবে

jobs:
  deploy:
    name: Deploy to Server
    runs-on: ubuntu-latest # GitHub একটি ভার্চুয়াল মেশিন ব্যবহার করবে

    steps:
      - name: Checkout the repository
        uses: actions/checkout@v4

      - name: Execute deployment script on server
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            # Step 1: Go to the project root and pull the latest code
            echo "==== Pulling latest code from GitHub... ===="
            cd /var/www/Beigelo
            git pull origin main

            # Step 2: Deploy Frontend
            echo "==== Starting Frontend Deployment... ===="
            cd frontend
            echo "Installing dependencies for Frontend..."
            npm install
            echo "Building Frontend..."
            npm run build
            echo "✅ Frontend Deployment Complete!"

            # Step 3: Deploy Admin Panel
            echo "==== Starting Admin Deployment... ===="
            cd ../admin  # Go back from frontend and into admin
            echo "Installing dependencies for Admin..."
            npm install
            echo "Building Admin..."
            npm run build
            echo "✅ Admin Deployment Complete!"

            # Step 4: Deploy Backend
            echo "==== Starting Backend Deployment... ===="
            cd ../backend # Go back from admin and into backend
            echo "Installing dependencies for Backend..."
            npm install
            echo "Restarting server with PM2..."
            pm2 restart beigelo_server
            echo "✅ Backend Deployment Complete!"
            echo "🚀 Deployment Successful!"
